// Incoming call handling has moved to CallNavStatus (RoomCallNavStatus.tsx).
// This stub keeps the Router.tsx import working without further patch changes.
export function IncomingCallNotification() {
  return null;
}
